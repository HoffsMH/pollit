"use strict";

const assert = require('assert');
const expect = require('chai').expect;
const app = require('../app/models/choice-set.js');

describe('ChoiceSet', () => {
  context("when given correct params", () => {
    beforeEach(() => {
      var taco = "ok";
    });
    it("makes up a valid choice set object", () => {

      let pry = require('pryjs');
      eval(pry.it);

    });
  });

});
